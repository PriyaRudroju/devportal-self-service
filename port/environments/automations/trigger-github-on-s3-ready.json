{
  "identifier": "trigger_github_on_s3_ready",
  "title": "Trigger GitHub When S3 Ready",
  "description": "When s3Bucket status changes pending to ready, dispatches provision-s3-bucket.yml on GitHub using catalog gitRef as the workflow ref (GitHub dispatch branch).",
  "trigger": {
    "type": "automation",
    "event": {
      "type": "ENTITY_UPDATED",
      "blueprintIdentifier": "s3Bucket"
    },
    "condition": {
      "type": "JQ",
      "expressions": [
        ".diff.before.properties.status == \"pending\"",
        ".diff.after.properties.status == \"ready\""
      ],
      "combinator": "and"
    }
  },
  "invocationMethod": {
    "type": "INTEGRATION_ACTION",
    "installationId": "{{GITHUB_INSTALLATION_ID}}",
    "integrationActionType": "dispatch_workflow",
    "integrationActionExecutionProperties": {
      "org": "{{GITHUB_ORG}}",
      "repo": "{{GITHUB_REPO}}",
      "workflow": "provision-s3-bucket.yml",
      "ref": "{{ .event.diff.before.properties.gitRef }}",
      "reportWorkflowStatus": true,
      "workflowInputs": {
        "bucket_name": "{{ .event.diff.before.properties.bucketName }}",
        "environment": "{{ .event.diff.before.properties.environment }}",
        "port_run_id": "{{ .event.diff.before.properties.portRunId }}"
      }
    }
  },
  "publish": true
}
