{
  "identifier": "provision_s3_request",
  "title": "Provision S3 Bucket",
  "icon": "Bucket",
  "description": "Request S3 bucket provisioning — creates catalog entity; GitHub dispatch runs via automation (legacy Sunset app)",
  "category": "Infra",
  "nodes": [
    {
      "identifier": "trigger",
      "title": "S3 Request Form",
      "config": {
        "type": "SELF_SERVE_TRIGGER",
        "userInputs": {
          "properties": {
            "bucket_name": {
              "title": "Bucket Name",
              "type": "string",
              "description": "Globally unique S3 bucket name"
            },
            "git_ref": {
              "title": "Git Branch",
              "type": "string",
              "description": "dev = stable dev branch. Feature branch names are for testing Port/Git changes only — Terraform still targets dev infrastructure.",
              "enum": {{GIT_REF_ENUM}},
              "default": "{{GIT_REF_DEFAULT}}"
            }
          },
          "required": ["bucket_name", "git_ref"]
        }
      }
    },
    {
      "identifier": "create_ready_entity",
      "title": "Create Ready Catalog Entity",
      "config": {
        "type": "UPSERT_ENTITY",
        "blueprintIdentifier": "s3Bucket",
        "mapping": {
          "identifier": "{{ .workflowRun.identifier }}",
          "title": "S3 {{ .outputs.trigger.bucket_name }}",
          "properties": {
            "bucketName": "{{ .outputs.trigger.bucket_name }}",
            "gitRef": "{{ .outputs.trigger.git_ref }}",
            "environment": "dev",
            "region": "{{AWS_REGION}}",
            "status": "ready",
            "requestedBy": "{{ .workflowRun.trigger.by.email }}",
            "portRunId": "{{ .workflowRun.identifier }}"
          }
        }
      }
    }
  ],
  "connections": [
    {
      "sourceIdentifier": "trigger",
      "targetIdentifier": "create_ready_entity"
    }
  ]
}
