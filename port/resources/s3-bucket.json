{
  "identifier": "s3Bucket",
  "title": "S3 Bucket",
  "icon": "Bucket",
  "schema": {
    "properties": {
      "bucketName": {
        "type": "string",
        "title": "Bucket Name"
      },
      "environment": {
        "type": "string",
        "title": "Environment"
      },
      "gitRef": {
        "type": "string",
        "title": "GitHub dispatch branch"
      },
      "region": {
        "type": "string",
        "title": "AWS Region",
        "default": "us-east-1"
      },
      "status": {
        "type": "string",
        "title": "Status",
        "enum": ["pending", "ready", "provisioned", "failed"],
        "default": "pending"
      },
      "requestedBy": {
        "type": "string",
        "title": "Requested By"
      },
      "portRunId": {
        "type": "string",
        "title": "Port Run ID"
      }
    },
    "required": ["bucketName", "environment"]
  }
}
