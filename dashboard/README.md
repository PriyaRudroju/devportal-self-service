# Local preview of the BannerHealth `$home` layout

```bash
python3 -m http.server 4173
```

http://localhost:4173

This is a visual stand-in for Port widgets. Live apply uses [`../port/pages/home.json`](../port/pages/home.json) in `port_orchestration_repo`.
